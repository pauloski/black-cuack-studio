# Secretos — NUNCA versionar. .dev.vars contiene el FLOW_SECRET_KEY.
.env
.env.local
.dev.vars

# Estado local de Wrangler / Pages
.wrangler/
node_modules/

.DS_Store

# wrangler.toml de la RAÍZ es solo para 'wrangler pages dev' local; en producción
# manda el panel. Se ancla a la raíz (/) para NO ignorar worker-cron/wrangler.toml,
# que sí se versiona (no lleva secretos, solo IDs de recursos).
/wrangler.toml

# imágenes fuente pesadas y sin uso (el sitio usa el CDN de Contentful)
images/product/

.dev.vars*
!.dev.vars.example
!.env.example
